# Remap CapsLock → Escape on the laptop's internal keyboard only.
# Device 0001:0001 = AT Translated Set 2 (i8042) — the built-in keyboard.
# External keyboards are excluded so their own firmware mappings are unaffected.
[ids]
0001:0001

[main]
capslock = esc
